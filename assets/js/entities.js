/*

export default class Reticle extends Phaser.GameObjects.Sprite
{
  constructor(config)
  {
    super(config.scene)
    this.sprite = config.scene.physics.add.sprite(800, 700, 'crosshair')
    .setOrigin(0.5, 0.5)
    .setDisplaySize(20,20);

    config.scene.add.existing(this);
  }
}
*/
